import { CHAT_MESSAGE } from "../actions/types";
const initialState = {
  chatMessage: null
};
export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
